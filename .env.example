# OpenRouter API key for bundled app
# Copy this file to .env and replace with your key:
#   cp .env.example .env
# Then build the app — the key will be baked in automatically.
OPENROUTER_API_KEY=sk-or-v1-your-key-here
